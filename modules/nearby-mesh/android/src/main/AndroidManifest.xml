<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <!--
    Google Nearby Connections needs the union of Bluetooth, Wi-Fi and location
    permissions. Which ones are actually requested at runtime depends on the API
    level - see NearbyMeshModule.requiredPermissions().
  -->

  <!-- Legacy Bluetooth (pre-Android 12). -->
  <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
  <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />

  <!-- Android 12+ (API 31) Bluetooth split. -->
  <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
  <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
  <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

  <!-- Android 13+ (API 33) Wi-Fi Direct / Aware. -->
  <uses-permission
    android:name="android.permission.NEARBY_WIFI_DEVICES"
    android:usesPermissionFlags="neverForLocation" />

  <!-- Nearby derives proximity from location on older API levels. -->
  <uses-permission
    android:name="android.permission.ACCESS_FINE_LOCATION"
    android:maxSdkVersion="32" />
  <uses-permission
    android:name="android.permission.ACCESS_COARSE_LOCATION"
    android:maxSdkVersion="32" />

  <!-- Wi-Fi hotspot / direct transports used by the cluster strategy. -->
  <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
  <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

</manifest>
